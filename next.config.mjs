
/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Removed to support API Routes
    /* images: {
        unoptimized: true,
    }, */
};

export default nextConfig;
